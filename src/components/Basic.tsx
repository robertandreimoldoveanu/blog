function Basic() {
  return <div>Basic</div>;
}

// left in place just for example purposes, not actually used
// shows how we can add custom components to MDX remote (in the [slug].tsx file)
export default Basic;
